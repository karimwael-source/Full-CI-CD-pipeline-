# Python
__pycache__/
*.py[cod]
*.egg-info/
.env
venv/
.venv/

# MLflow
mlruns/
mlartifacts/

# DVC — the actual data files are ignored by Git.
# Only the .dvc pointer files get committed.
/data
*.dvc
!*.dvc   # re-include .dvc pointer files
.dvc/cache
.dvc/tmp

# Model files
*.pkl
*.pt
*.h5
model_info.txt   # generated during CI — not committed